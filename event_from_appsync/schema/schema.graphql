type Event {
    result: String
}

type Query {
    getEvent: [Event]
}

type Mutation {
    createEvent(event: String!): Event
    deleteEvent(id:String!):Event
    updateEvent(event: String!, id: String!): Event
}